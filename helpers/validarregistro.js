/*let inputs = document.querySelectorAll("formulario_entrada")
imputs.map((input)=> {
    console.log(input)})
*/

//inputs.map(funtion(){})

let imputs = document.getElementsByClassName
("formulario_entrada")

let arregloImputs = [...imputs]
console.log(imputs)
console.log(arregloImputs)

arregloImputs.map((imput)=> {
    console.log(imput)
})

// ... expres opereitor: convierte a un elemnto iterable o que se puede contar
